# THIS FILE IS AUTOMATICALLY GENERATED BY CARGO
#
# When uploading crates to the registry Cargo will automatically
# "normalize" Cargo.toml files for maximal compatibility
# with all versions of Cargo and also rewrite `path` dependencies
# to registry (e.g., crates.io) dependencies
#
# If you believe there's an error in this file please file an
# issue against the rust-lang/cargo repository. If you're
# editing this file be aware that the upstream Cargo.toml
# will likely look very different (and much more reasonable)

[package]
edition = "2018"
name = "openvino-sys"
version = "0.3.1"
authors = ["OpenVINO Project Developers"]
links = "inference_engine_c_api"
include = ["/Cargo.toml", "/README.md", "/build.rs", "/src", "/upstream/inference-engine/ie_bridges/c/include"]
description = "Low-level bindings for OpenVINO (use the `openvino` crate for easier-to-use bindings)."
documentation = "https://docs.rs/openvino-sys"
readme = "README.md"
license = "Apache-2.0"
repository = "https://github.com/intel/openvino-rs"
[package.metadata.docs.rs]
features = ["runtime-linking"]
[dependencies.lazy_static]
version = "1.4"
optional = true

[dependencies.libloading]
version = "0.7"
optional = true

[dependencies.openvino-finder]
version = "0.3.1"
[build-dependencies.cmake]
version = "0.1.45"

[build-dependencies.openvino-finder]
version = "0.3.1"

[features]
all = ["cpu", "gpu", "gna", "hetero", "multi", "myriad"]
cpu = []
default = ["cpu"]
dynamic-linking = []
from-source = []
gna = []
gpu = []
hetero = []
multi = []
myriad = []
runtime-linking = ["libloading", "lazy_static"]
